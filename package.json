{
  "name": "server-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "deploy": "gcloud app deploy --project projectdenimar01",

    "git:push": "git add . && git commit -am 'implemented version 1.0.0.11' && git push",
    "git:tagging": "git tag -a v1.0.0.11 -m 'my version 1.0.0.11' && git push origin --tags",
    "kubernetes:rollout": "gcloud container clusters get-credentials cluster-denimar01 --zone us-central1-a --project projectdenimar01 && gcloud deployment-manager deployments update microservice01",
    "git": "npm run git:push && npm run git:tagging && npm run kubernetes:rollout"
  },
  "comment": {
    "about deploy": {
      "--no-promote": "evoid to publish https://server01-dot-projectdenimar01.appspot.com",
      "externalIP": "130.211.234.187",
      "#1": "após run deploy, eu tenho que entrar via ssh rodar 'kubectl get pods' pegar o pod e rodar 'kubectl delete pod <pod name>'. Será que não tem uma forma de fazer isso automaticamente???"
    }  
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3"
  }
}